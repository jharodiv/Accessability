import 'package:flutter/material.dart';

class ChatCenterEmpty extends StatelessWidget {
  const ChatCenterEmpty({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: SizedBox(
        width: 80,
        height: 80,
      ),
    );
  }
}
